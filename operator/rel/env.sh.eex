#!/bin/sh

# Clamp containerd's unlimited fd rlimit before ERTS sizes fd tables.
# The operator needs very few fds and runs under a small memory limit,
# so clamp much lower than the data plane.
if [ "$(ulimit -n)" -gt 65536 ] 2>/dev/null; then
  ulimit -n 65536
fi
