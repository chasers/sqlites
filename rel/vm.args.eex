## Sized for ~100k database server processes per node plus overhead.
+P 2000000

## Pin the Erlang distribution listener so it can be a fixed pod port.
-kernel inet_dist_listen_min 9100 inet_dist_listen_max 9100
