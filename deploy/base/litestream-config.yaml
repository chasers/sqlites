apiVersion: v1
kind: ConfigMap
metadata:
  name: litestream-config
  namespace: sqlites
data:
  litestream.yml: |
    # Databases are registered dynamically by the data plane over the
    # control socket (litestream register); no static list here.
    dbs: []
