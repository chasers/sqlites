apiVersion: v1
kind: Service
metadata:
  name: sqlites-external
  namespace: sqlites
spec:
  type: NodePort
  selector:
    app: sqlites
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      nodePort: 30080
